# Simple script to test Chapter 20 renderer - copy cells to notebook in order

# This file shows the correct cell order for chapter_20_integration_and_projects.ipynb
# Copy each section to a new cell in the notebook

# CELL 1 (markdown): Title and intro
# CELL 2 (code): imports
# CELL 3 (code): Core classes (Vec3, Ray, Scene, etc.)
# CELL 4 (code): IntegratedRenderer class
# CELL 5 (code): create_demo_scene function
# CELL 6 (code): Render and display

# The complete working example is below - just needs to be in separate cells

import numpy as np
import matplotlib.pyplot as plt
import math
import random
from typing import List, Optional
from dataclasses import dataclass, field
from enum import Enum

# Vec3, Ray, HitRecord, Material, Sphere, Light, Camera, Scene classes here
# IntegratedRenderer class here
# create_demo_scene function here
# Finally: scene = create_demo_scene(); renderer.render(scene)
